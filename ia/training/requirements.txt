# Dépendances pour le fine-tuning LoRA du chatbot UFR SI
# (Installer dans un environnement virtuel séparé du backend Django)

torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.28.0
datasets>=2.18.0
bitsandbytes>=0.43.0  ; sys_platform == 'linux'   # Quantization 4-bit (Linux/CUDA)
sentencepiece>=0.2.0
tensorboard>=2.16.0
