# Dépendances du microservice d'inférence
# (Léger : pas de torch si on utilise le mode mock)

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# Pour l'inférence réelle (Phase 5 production)
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.28.0
